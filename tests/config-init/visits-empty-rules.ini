# Set these properties
redcap_api_url =
data_source_api_token =

# Change these values if necessary
web_script = visits.php
web_script_url = http://localhost/visits.php

db_connection =

# Set these if you want to receive
# e-mail notifications of errors
email_from_address = 
email_subject = 
email_to_list =

# Changing these properties is optional
ssl_verify = 0
ca_cert_file = 

log_file = ../logs/visits-empty-rules.log
web_script_log_file = 
print_logging = 0

allowed_servers = localhost
batch_size = 10
label_view_suffix =
table_prefix = 

timezone = 


# These properties should not be changed
transform_rules_source = 2
transform_rules_file = visits-empty-rules.txt

db_ssl = 0
post_processing_sql_file = visits.sql

