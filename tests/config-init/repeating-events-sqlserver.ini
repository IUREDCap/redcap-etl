# Set these properties
redcap_api_url = 
data_source_api_token = 

# Changing these REDCap connection properties is optional
ssl_verify = 0
ca_cert_file = 

email_from_address = 
email_subject = 
email_to_list =

db_connection = 

# Set to 1 if you want the connection to the server to be encrypted
db_ssl = 0

# Set to 1 to force verification of the cert installed on the SQL Server instance
db_ssl_verify = 0

log_file = ../logs/sql-server-test.log
print_logging = 0

transform_rules_source = 2
transform_rules_file = repeating-events-rules.txt

batch_size = 10

label_views = 1
label_view_suffix = _label_view
table_prefix =

