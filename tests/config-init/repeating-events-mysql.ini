# Set these properties
redcap_api_url = 
data_source_api_token = 

# Changing these properties is optional
ssl_verify = 1
ca_cert_file = 

email_from_address = 
email_subject = 
email_to_list =

db_connection = MySQL:127.0.0.1:etl_user:etlPassword:etl_test

# Set this to 0 if SSL not configures for your MySQL server
db_ssl = 1

log_file = ../logs/repeating-events.log
print_logging = 0

transform_rules_source = 2
transform_rules_file = repeating-events-rules.txt

batch_size = 10

label_view_suffix = _label_view
table_prefix =

